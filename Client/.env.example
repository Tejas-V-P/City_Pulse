# Copy this file to .env.local for local development, or set VITE_API_URL as
# an environment variable in your hosting platform (e.g. Vercel project settings).

# Base URL of the deployed backend API (no trailing slash).
# Leave unset locally to fall back to http://localhost:5000
VITE_API_URL=http://localhost:5000
